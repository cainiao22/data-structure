#ifndef COMMON_H
#define COMMON_H


#define MIN(a, b) (a) < (b) ? (a) : (b)

#define max(a, b) (a) > (b) ? (a) : (b)

#define INF 0x7fffffff

//伴随网络用，有界无限大
#define INF_LIMITED 0x7ffffff

#define MAX 100

//顶点数量
#define NMAX 100

//边的数量
#define MMAX 100


#endif // COMMON_H
